const db = require('../models');

class UsersController {

  // GET /users
  index(req, res) {

  }

  // GET /users/:id
  show(req, res) {

  }

  // PUT /users/:id
  update(req, res) {

  }

  // DELETE /users/:id
  delete(req, res) {

  }
}

module.exports = new UsersController();
